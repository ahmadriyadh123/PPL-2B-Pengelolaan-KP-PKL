PORT=60000
CHOKIDAR_USEPOLLING=true
# Use a relative base so the app works when hosted under a subpath (e.g. /500) or at root
# Leading './' makes requests resolve relative to the current app path
REACT_APP_API_GATEWAY_URL=./
NODE_OPTIONS='--openssl-legacy-provider'