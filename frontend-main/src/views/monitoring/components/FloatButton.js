/* eslint-disable react/prop-types */
import React from 'react'; import { Button, Tooltip } from 'antd';  const FloatButton = ({ icon, onClick, tooltip, type, style, ...props }) => {   const btn = (     <Button       shape="circle"       size="large"       type={type || 'default'}       icon={icon}       onClick={onClick}       style={{         position: 'fixed',         right: 24,         bottom: 24,         zIndex: 1000,         boxShadow: '0 2px 8px rgba(0,0,0,0.15)',         width: 48,         height: 48,         display: 'flex',         justifyContent: 'center',         alignItems: 'center',         ...style       }}       {...props}     />   );   if (tooltip) {     return <Tooltip title={tooltip} placement="left">{btn}</Tooltip>;   }   return btn; };  export default FloatButton;
